# Database (SQLite). When running API from monorepo (e.g. turbo run dev), cwd is apps/api — use path relative to that.
DATABASE_URL="file:../../packages/database/prisma/dev.db"

# API
PORT=4000
BASE_URL="http://localhost:4000"

# Optional: API key header name (default: x-api-key)
# API_KEY_HEADER=x-api-key
